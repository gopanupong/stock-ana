# Damodaran DCF Analyzer

แอปพลิเคชันวิเคราะห์มูลค่าหุ้นด้วยวิธี Discounted Cash Flow (DCF) ตามแบบฉบับ Aswath Damodaran โดยใช้พลังของ Google Gemini AI

## 🔑 การตั้งค่า API Key (สำคัญมาก)

ก่อนรันโปรเจกต์ คุณต้องมี API Key จาก Google Gemini ก่อน

### 1. สำหรับรันในเครื่อง (Local)
สร้างไฟล์ใหม่ชื่อ `.env` (ไม่มีชื่อข้างหน้า) ไว้ที่โฟลเดอร์นอกสุด และใส่โค้ดดังนี้:
```env
API_KEY=ใส่รหัส_Gemini_API_Key_ของคุณที่นี่
```

### 2. สำหรับ Deploy บน Vercel
1. ไปที่หน้า Dashboard ของโปรเจกต์ใน Vercel
2. ไปที่ **Settings** > **Environment Variables**
3. เพิ่มตัวแปร:
   * **Key:** `API_KEY`
   * **Value:** (ใส่รหัส Gemini API Key ของคุณ)

## 🚀 การรันโปรเจกต์

1.  **ติดตั้ง Node.js** (แนะนำเวอร์ชัน 18 หรือสูงกว่า)
2.  **ติดตั้ง Dependencies**:
    ```bash
    npm install
    ```
3.  **รันเซิร์ฟเวอร์**:
    ```bash
    npm run dev
    ```

## ☁️ การ Deploy บน Vercel

1.  อัปโหลดโค้ดขึ้น **GitHub**, **GitLab**, หรือ **Bitbucket**
2.  ไปที่ [Vercel Dashboard](https://vercel.com/dashboard) แล้วกด **"Add New..."** -> **"Project"**
3.  เลือก Repository ที่คุณอัปโหลดไว้
4.  ตรวจสอบ Environment Variables ให้เรียบร้อยตามข้อ 2 ด้านบน
5.  กด **Deploy** 🚀